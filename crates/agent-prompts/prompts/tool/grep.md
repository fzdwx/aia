Fast content search tool that works across the workspace.

Usage:
- Searches file contents using regular expressions.
- Supports full regex syntax.
- The optional `glob` parameter filters which files are searched.
- Respects `.gitignore` and skips common large directories such as `.git`, `node_modules`, and `target`.
- Use this tool when you need to find files containing specific patterns.
