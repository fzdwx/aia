Writes a file to the local filesystem.

Usage:
- This tool overwrites the target file if it already exists.
- It creates parent directories when they do not already exist.
- Prefer editing existing files when possible; use this tool when you truly need to create or fully replace file contents.
