Use the `apply_patch` tool to edit files with a structured patch format.

Usage:
- Supports `Update File`, `Add File`, `Delete File`, and `Move to` operations.
- Accepts either `patch` or `patchText`; when both are provided, they must match exactly.
- Use this tool when you need to apply coordinated edits across one or more files in a single patch.
