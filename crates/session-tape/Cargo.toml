[package]
name = "session-tape"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
aia-config = { path = "../aia-config" }
agent-core = { path = "../agent-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
