# desktop wrapper

桌面应用不应复制代理逻辑，而应复用同一个 shared runtime。

## 约束

- 桌面层只负责窗口、输入、通知、文件选择与系统集成
- 会话、工具、模型、交接、压缩仍由共享运行时负责
- 终端界面与桌面界面消费同一种事件流与同一种会话磁带

## 结果

这样可以避免两套代理逻辑长期漂移：

- 终端界面先成熟
- 桌面壳后接入
- 共享运行时仍是唯一真实执行核心
